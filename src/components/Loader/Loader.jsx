import { Grid, Typography } from "@mui/material";

const Loader = () => {
  return (
    <Grid container>
      <Typography variant="h4">Loading...</Typography>
    </Grid>
  );
};

Loader.propTypes = {};

export { Loader };
