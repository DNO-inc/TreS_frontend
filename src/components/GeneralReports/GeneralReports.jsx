import { Typography } from "@mui/material";

const GeneralReports = () => {
  return (
    <Grid container>
      <Typography variant="h1">General Reports</Typography>
    </Grid>
  );
};

GeneralReports.propTypes = {};

export { GeneralReports };
