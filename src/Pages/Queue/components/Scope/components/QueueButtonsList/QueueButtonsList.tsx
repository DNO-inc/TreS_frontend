import { FC } from "react";

import Box from "@mui/material/Box";

interface QueueButtonsListProps {}

const QueueButtonsList: FC<QueueButtonsListProps> = () => {
  return <Box sx={{ mb: 3 }}>Queue buttons list</Box>;
};

export { QueueButtonsList };
